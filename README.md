# railway-map

JR 東日本などの鉄道路線を「路線図（模式図）」として表示する Web アプリ。趣味で開発しているプロジェクトです。

## 概要

地理院地図のような背景地図は使わず、駅を見やすく整理した図上座標を SVG で描く、スキマティックな路線図を表示する Web サイト。駅・路線のデータはコード内（`src/diagram/data.ts`）に手で定義する。

## 設計方針

- コスト安全性（最重要）: クラウド利用時の高額請求（従量課金の爆発）を構造的に回避する
- インフラの可搬性: Cloudflare と自宅 Docker 環境のどちらでも動かせる構成にする
- 自動デプロイ: GitHub への push をトリガーとした CI/CD を前提とする

### コスト安全性の考え方
- ホスティングは Cloudflare Pages（転送量無料枠が実質無制限）
- サーバーレス層（Workers / D1）は無料プランに留める（超過時はエラー停止で課金なし）
- 従量課金型の外部サービスは原則使わない

## 技術スタック

### フロントエンド
- ビルド基盤: Vite + React + TypeScript
- 描画: SVG（路線図の模式図をコンポーネントで描画）
- 状態/キャッシュ管理: TanStack Query（将来のバックエンド連携を見据えて初期導入）

### データ戦略
- 駅・路線データは `src/diagram/data.ts` に定義する
  - `STATIONS`: 駅の図上座標（id・名前・座標・ラベル向き・主要駅フラグ）
  - `LINES`: 路線（通過順の駅 id と路線カラー）
  - 既存の駅 id を複数路線で共有すると、その駅が乗換駅として自然に重なる

### ホスティング
- Cloudflare Pages（GitHub 連携で自動デプロイ）
  - ビルドコマンド: npm run build
  - 出力ディレクトリ: dist

### バックエンド & ストレージ（将来の拡張ロードマップ・現時点では未実装）
- サーバーレス: Cloudflare Workers
- データベース: Cloudflare D1（SQLite ベース）

## セットアップ

### 必要環境
- Node.js v20 以上（LTS 推奨）

### 開発

```bash
npm install
npm run dev
```

### ビルド

```bash
npm run build
```

## デプロイ
main ブランチへの push をトリガーに Cloudflare Pages で自動デプロイされる。

## ロードマップ
- [x] 路線図（模式図）を表示する
- [x] 複数路線・乗換駅を描画する
- [ ] 路線・駅を増やす（総武・常磐・東海道・新幹線 ほか）
- [ ] 駅クリックで情報を表示する
- [ ] バックエンド（Workers / D1）の導入
- [ ] 自宅 Docker 環境での動作対応
