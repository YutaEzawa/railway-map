/**
 * 東北6県＋関東一都六県の鉄道（JR 東日本＋私鉄・三セク）の
 * 路線・駅データを生成するビルドスクリプト。
 *
 * 入力（無料の公開データ。リポジトリには含めないので各自取得する）:
 *   - 国土数値情報 鉄道データ N02（GeoJSON。全国版）
 *       https://nlftp.mlit.go.jp/ksj/gml/data/N02/N02-23/N02-23_GML.zip
 *       展開後の N02-23_RailroadSection.geojson / N02-23_Station.geojson
 *   - 都道府県境界 GeoJSON（対象都県ポリゴンの切り出しに使用）
 *       https://raw.githubusercontent.com/dataofjapan/land/master/japan.geojson
 *
 * 使い方:
 *   N02_DIR=/path/to/N02/UTF-8 JAPAN_GEOJSON=/path/to/japan.geojson \
 *     node scripts/build-railways.cjs
 *   （既定では ./.data/ 配下を参照する）
 *
 * 出力（リポジトリにコミットする静的データ）:
 *   - public/data/railways.geojson  路線（路線ごとの MultiLineString、category=jr|private）
 *   - public/data/stations.geojson  駅（Point。isJr/isPrivate/major）
 */
require('./load-env.cjs')
const fs = require('fs')
const path = require('path')
const { readRows, toSections, toTrains } = require('./lines-csv.cjs')

const N02_DIR = process.env.N02_DIR || path.resolve(__dirname, '../.data/N02/UTF-8')
const JAPAN_GEOJSON =
  process.env.JAPAN_GEOJSON || path.resolve(__dirname, '../.data/japan.geojson')
const OUT_DIR = path.resolve(__dirname, '../public/data')

// 駅別の停車本数（平日・片方向）。急行/各停で停車パターンが違う路線の駅サイズ用。
// train-counts/scrape-station-stops.cjs が生成。{ 路線: { 駅名: { total, types } } }。無くても可。
const STOPS_PATH = path.resolve(__dirname, '../train-counts/station-stops.json')
const STOP_TRAINS = fs.existsSync(STOPS_PATH)
  ? JSON.parse(fs.readFileSync(STOPS_PATH, 'utf8'))
  : {}

/**
 * 駅の停車本数を返す。その駅が属する路線のうち合計本数が最大のものの { total, types }
 * （データが無ければ null）。乗換駅で複数路線にデータがある場合は本数の多い方を採用。
 */
function stopTrainsFor(name, lineList) {
  let best = null
  for (const line of lineList) {
    const rec = STOP_TRAINS[line] && STOP_TRAINS[line][name]
    if (rec && typeof rec.total === 'number' && (best === null || rec.total > best.total)) best = rec
  }
  return best
}

/** JR グループ（凡例の JR/私鉄 切替用）。旅客鉄道6社すべてを JR 扱いにする。 */
const JR_OPERATORS = new Set([
  '東日本旅客鉄道', '北海道旅客鉄道', '東海旅客鉄道', '西日本旅客鉄道', '四国旅客鉄道', '九州旅客鉄道',
])
/**
 * 対象都県（japan.geojson の id）。全国 47 都道府県。
 * 北海道=1 / 青森=2 / 岩手=3 / 宮城=4 / 秋田=5 / 山形=6 / 福島=7（東北）
 * 茨城=8 / 栃木=9 / 群馬=10 / 埼玉=11 / 千葉=12 / 東京=13 / 神奈川=14（関東）
 * 新潟=15 / 富山=16 / 石川=17 / 福井=18 / 山梨=19 / 長野=20 / 岐阜=21 / 静岡=22 / 愛知=23（中部）
 * 三重=24 / 滋賀=25 / 京都=26 / 大阪=27 / 兵庫=28 / 奈良=29 / 和歌山=30（近畿）
 * 鳥取=31 / 島根=32 / 岡山=33 / 広島=34 / 山口=35（中国）
 * 徳島=36 / 香川=37 / 愛媛=38 / 高知=39（四国）
 * 福岡=40 / 佐賀=41 / 長崎=42 / 熊本=43 / 大分=44 / 宮崎=45 / 鹿児島=46（九州）/ 沖縄=47
 */
const TARGET_PREF_IDS = Array.from({ length: 47 }, (_, i) => i + 1)

/**
 * 対象都県に地物がかかっていても対象外にする事業者。
 * 青函トンネル竜飛斜坑線（体験坑道のケーブルカー。旧線跡の観光施設で通常の鉄道網ではない）。
 */
const EXCLUDED_OPERATORS = new Set(['一般財団法人青函トンネル記念館'])

/** 本数が見つからない路線の既定値。 */
const DEFAULT_TRAINS = 30

/** 同名駅を同一駅とみなす最大距離（度。約 2km）。これを超えると別駅として分割。 */
const STATION_CLUSTER_DIST = 0.02

// 区間定義・本数は data/lines.csv（手編集）から読み込む。
const CSV_ROWS = readRows()
const LINE_SECTIONS = toSections(CSV_ROWS) // { 路線名: [{label, stations}] }
const TRAINS = toTrains(CSV_ROWS) // { 区間 or 路線: 本数 }

/**
 * 複々線で各駅停車・快速を並べて表示する路線（並走サービス）。
 * N02 の 1 本の路線ジオメトリを、サービスごとに line-offset でずらして 2 本描く。
 * 対象区間（駅集合）と各サービスの本数は CSV の各サービス行（路線名＝サービス名）から取る。
 * baseN02: 元の N02 路線名（displayName）。offset: 並走の左右（+/-1）。
 */
const SERVICE_LINES = {
  総武線: [
    // 緩行は三鷹〜千葉まで通しで「中央・総武線各駅停車」（offset は中央線と揃えて +1）。
    { name: '中央・総武線各駅停車', color: '#FFD400', offset: 1 },
    { name: '総武線快速', color: '#0072BC', offset: -1 },
  ],
  常磐線: [
    { name: '常磐線各駅停車', color: '#00B261', offset: 1 },
    { name: '常磐線快速', color: '#1FA8E0', offset: -1 },
  ],
  // 赤羽線（赤羽〜池袋）は埼京線・湘南新宿ライン共用。lines.csv の湘南新宿ライン駅欄でゾーンを定義。
  埼京線: [
    { name: '埼京線', color: '#00AC9A', offset: 1 },
    { name: '湘南新宿ライン', color: '#F68B1E', offset: -1 },
  ],
}
// 並走対象区間の駅集合（baseN02 -> Set(駅名)）。各サービス行の駅の和集合。
const SERVICE_STATION_SET = {}
for (const [base, services] of Object.entries(SERVICE_LINES)) {
  const set = (SERVICE_STATION_SET[base] = new Set())
  for (const sv of services) {
    // 区間分割されている場合も含めて全行を収集する。
    const rows = CSV_ROWS.filter((r) => r.line === sv.name)
    for (const row of rows) for (const st of row.stations) set.add(st)
  }
}

/**
 * 系統が重なる JR 幹線（東北線・東海道線・中央線）を「通称」ごとに描き分ける定義。
 * N02 は 1 つの物理路線名に複数系統（京浜東北/宇都宮、京浜東北/東海道、快速/各停 等）を
 * 収録しているため、駅集合で「ゾーン」に分け、各ゾーンで該当する通称サービスを引く。
 * 同一ゾーンに複数サービスがある区間（共用複々線）は line-offset で並べて描く。
 * ゾーン判定はセグメント中点の最寄り駅（そのゾーンの駅集合内）で行う。
 * 本数はサービス名で CSV からひく（無ければ既定）。色はここで指定。
 */
const CORRIDORS = {
  東北線: [
    {
      // 東京〜赤羽: 京浜東北線と宇都宮線（東北本線中距離）が共用する主線。
      label: '東北本線(東京〜赤羽)',
      services: [
        { name: '京浜東北線', color: '#00BAE8', offset: 1 },
        { name: '宇都宮線', color: '#F68B1E', offset: -1 },
      ],
      stations: '東京 神田 秋葉原 御徒町 上野 鶯谷 日暮里 西日暮里 田端 上中里 王子 東十条 尾久 赤羽'.split(' '),
    },
    {
      // 赤羽〜大宮: 京浜東北線・宇都宮線に加え湘南新宿ラインも共用する区間。
      label: '東北本線(赤羽〜大宮)',
      services: [
        { name: '京浜東北線', color: '#00BAE8', offset: 2 },
        { name: '宇都宮線', color: '#F68B1E', offset: 0 },
        { name: '湘南新宿ライン', color: '#F68B1E', offset: -2 },
      ],
      stations: '川口 西川口 蕨 南浦和 浦和 北浦和 与野 さいたま新都心 大宮'.split(' '),
    },
    {
      // 大宮以北: 宇都宮線（中距離）単独。
      label: '宇都宮線(大宮〜栗橋)',
      services: [{ name: '宇都宮線', color: '#F68B1E', offset: 0 }],
      stations: '土呂 東大宮 蓮田 白岡 新白岡 久喜 東鷲宮 栗橋'.split(' '),
    },
    {
      // 栗橋〜黒磯: 宇都宮線（愛称の適用範囲。中距離電車の終点は黒磯）単独。
      label: '宇都宮線(栗橋〜黒磯)',
      services: [{ name: '宇都宮線', color: '#F68B1E', offset: 0 }],
      stations:
        '古河 野木 間々田 小山 小金井 自治医大 石橋 雀宮 宇都宮 岡本 宝積寺 氏家 蒲須坂 片岡 矢板 野崎 西那須野 那須塩原 黒磯'.split(
          ' ',
        ),
    },
    {
      // 黒磯以北（東北への拡大で追加）: 愛称の付かない東北本線（普通列車）単独。
      // 区間ごとに本数が大きく変わるため trainSection で区間分割する（サービス名は統一）。
      // ゾーン判定は最寄り駅の直線距離で行うため、隣接ゾーンへの誤分類（＝区間分割時の
      // 連結ギャップ）を防ぐよう各ゾーンの駅をできるだけ密に列挙する。
      label: '東北本線(黒磯〜郡山)',
      services: [{ name: '東北本線', color: '#8B4F9F', offset: 0, trainSection: '黒磯〜郡山' }],
      stations:
        '黒磯 高久 黒田原 豊原 白坂 新白河 白河 久田野 泉崎 矢吹 鏡石 須賀川 安積永盛 郡山'.split(' '),
    },
    {
      label: '東北本線(郡山〜福島)',
      services: [{ name: '東北本線', color: '#8B4F9F', offset: 0, trainSection: '郡山〜福島' }],
      stations: '郡山 日和田 五百川 本宮 杉田 二本松 安達 松川 金谷川 南福島 福島'.split(' '),
    },
    {
      label: '東北本線(福島〜白石)',
      services: [{ name: '東北本線', color: '#8B4F9F', offset: 0, trainSection: '福島〜白石' }],
      stations: '福島 東福島 伊達 桑折 藤田 貝田 越河 白石'.split(' '),
    },
    {
      // 仙台近郊は普通列車の本数が急に増えるため白石で区切る。
      label: '東北本線(白石〜仙台)',
      services: [{ name: '東北本線', color: '#8B4F9F', offset: 0, trainSection: '白石〜仙台' }],
      stations: '白石 北白川 東白石 大河原 船岡 槻木 岩沼 館腰 名取 南仙台 太子堂 長町 仙台'.split(' '),
    },
    {
      label: '東北本線(仙台〜小牛田)',
      services: [{ name: '東北本線', color: '#8B4F9F', offset: 0, trainSection: '仙台〜小牛田' }],
      stations:
        '仙台 東仙台 陸前山王 岩切 国府多賀城 塩釜 新利府 利府 松島 高城町 愛宕 品井沼 鹿島台 松山町 小牛田'.split(
          ' ',
        ),
    },
    {
      label: '東北本線(小牛田〜一ノ関)',
      services: [{ name: '東北本線', color: '#8B4F9F', offset: 0, trainSection: '小牛田〜一ノ関' }],
      stations: '小牛田 田尻 瀬峰 梅ヶ沢 新田 石越 油島 花泉 清水原 有壁 一ノ関'.split(' '),
    },
    {
      label: '東北本線(一ノ関〜北上)',
      services: [{ name: '東北本線', color: '#8B4F9F', offset: 0, trainSection: '一ノ関〜北上' }],
      stations: '一ノ関 山ノ目 平泉 前沢 陸中折居 水沢 金ヶ崎 六原 北上'.split(' '),
    },
    {
      label: '東北本線(北上〜盛岡)',
      services: [{ name: '東北本線', color: '#8B4F9F', offset: 0, trainSection: '北上〜盛岡' }],
      stations:
        '北上 村崎野 花巻 花巻空港 石鳥谷 日詰 紫波中央 古館 矢幅 岩手飯岡 仙北町 盛岡'.split(' '),
    },
    {
      // 赤羽〜大宮の別線（武蔵浦和経由）: 埼京線単独。主線と分岐するため branch。
      label: '埼京線(赤羽〜大宮)',
      branch: true,
      services: [{ name: '埼京線', color: '#00AC9A', offset: 0 }],
      stations: '浮間舟渡 北赤羽 戸田公園 戸田 北戸田 武蔵浦和 中浦和 南与野 与野本町 北与野'.split(' '),
    },
  ],
  東海道線: [
    {
      // 東京〜横浜: 京浜東北線と東海道線（中距離）が共用。
      label: '東海道本線(東京〜横浜)',
      services: [
        { name: '京浜東北線', color: '#00BAE8', offset: 1 },
        { name: '東海道線', color: '#F68B1E', offset: -1, trainSection: '東京〜横浜' },
      ],
      stations: '有楽町 新橋 浜松町 田町 高輪ゲートウェイ 品川 大井町 大森 蒲田 川崎 鶴見 新子安 東神奈川'.split(' '),
    },
    {
      // 横浜〜大船: 東海道線・横須賀線・湘南新宿ラインが並走（N02 は東海道線でまとめている区間）。
      // 保土ヶ谷・東戸塚（横須賀の駅）もここに含め、3系統を並走描画する。
      // 横須賀は offset 0（大船〜久里浜の単独区間と揃えて連続させる）、東海道を -2、湘南新宿を +2。
      label: '東海道・横須賀・湘南新宿(横浜〜大船)',
      services: [
        { name: '東海道線', color: '#F68B1E', offset: -2, trainSection: '横浜〜大船' },
        { name: '横須賀線', color: '#0072BC', offset: 0 },
        { name: '湘南新宿ライン', color: '#F68B1E', offset: 2 },
      ],
      stations: '横浜 保土ヶ谷 東戸塚 戸塚 大船'.split(' '),
    },
    {
      // 大船〜小田原: 東海道線（中距離）単独。湘南エリア。
      label: '東海道線(大船〜小田原)',
      services: [{ name: '東海道線', color: '#F68B1E', offset: 0, trainSection: '大船〜小田原' }],
      stations: '藤沢 辻堂 茅ヶ崎 平塚 大磯 二宮 国府津 鴨宮 小田原'.split(' '),
    },
    {
      // 小田原〜熱海: 東海道線（中距離）単独。本数が減少する末端区間。
      label: '東海道線(小田原〜熱海)',
      services: [{ name: '東海道線', color: '#F68B1E', offset: 0, trainSection: '小田原〜熱海' }],
      stations: '早川 根府川 真鶴 湯河原 熱海'.split(' '),
    },
    {
      // 品鶴線（東京方の横須賀線・湘南新宿）: 横須賀線・湘南新宿ライン共用。主線（鶴見・川崎）の近くを
      // 内陸で並走するため branch 指定（端点が品鶴駅の区間は主線へ流さない）。
      // 保土ヶ谷・東戸塚は東海道と並走するため上の横浜〜大船ゾーン側に入れる。
      label: '横須賀・湘南新宿(品鶴線)',
      branch: true,
      services: [
        { name: '横須賀線', color: '#0072BC', offset: 0 },
        { name: '湘南新宿ライン', color: '#F68B1E', offset: 2 },
      ],
      stations: '西大井 武蔵小杉 新川崎'.split(' '),
    },
    {
      // 相鉄・JR直通線（東海道貨物線経由）。
      label: '相鉄・JR直通線',
      branch: true,
      services: [{ name: '相鉄・JR直通線', color: '#6A1B7A', offset: 0 }],
      stations: '羽沢横浜国大'.split(' '),
    },
  ],
  中央線: [
    {
      // 御茶ノ水〜三鷹: 中央線快速と中央・総武線各駅停車の複々線。
      label: '中央複々線(御茶ノ水〜三鷹)',
      services: [
        // 緩行は総武線側と側を揃える（緩行 +1 / 快速 -1）。
        { name: '中央線快速', color: '#F15A22', offset: -1 },
        { name: '中央・総武線各駅停車', color: '#FFD400', offset: 1 },
      ],
      stations: '御茶ノ水 水道橋 飯田橋 市ヶ谷 四ツ谷 信濃町 千駄ヶ谷 代々木 新宿 大久保 東中野 中野 高円寺 阿佐ヶ谷 荻窪 西荻窪 吉祥寺 三鷹'.split(' '),
    },
    {
      // 東京〜御茶ノ水: 中央線快速単独。
      label: '中央線快速(東京〜御茶ノ水)',
      services: [{ name: '中央線快速', color: '#F15A22', offset: 0 }],
      stations: '東京 神田'.split(' '),
    },
    {
      // 三鷹〜高尾: 中央線快速単独。
      label: '中央線快速(三鷹〜高尾)',
      services: [{ name: '中央線快速', color: '#F15A22', offset: 0 }],
      stations: '武蔵境 東小金井 武蔵小金井 国分寺 西国分寺 国立 立川 日野 豊田 八王子 西八王子 高尾 相模湖 藤野'.split(' '),
    },
    {
      // 高尾以西（中部への拡大で追加）: 愛称の付かない中央本線（普通列車）単独。
      // 東北本線と同様、trainSection で区間分割する（サービス名は統一）。
      label: '中央本線(高尾〜甲府)',
      services: [{ name: '中央本線', color: '#D2691E', offset: 0, trainSection: '高尾〜甲府' }],
      stations:
        '上野原 四方津 梁川 鳥沢 猿橋 大月 初狩 笹子 甲斐大和 勝沼ぶどう郷 塩山 東山梨 山梨市 春日居町 石和温泉 酒折 甲府'.split(
          ' ',
        ),
    },
    {
      label: '中央本線(甲府〜塩尻)',
      services: [{ name: '中央本線', color: '#D2691E', offset: 0, trainSection: '甲府〜塩尻' }],
      stations:
        '甲府 竜王 塩崎 韮崎 新府 穴山 日野春 長坂 小淵沢 信濃境 富士見 すずらんの里 青柳 茅野 上諏訪 下諏訪 岡谷 川岸 辰野 みどり湖 信濃川島 小野 塩尻'.split(
          ' ',
        ),
    },
  ],
}
// 駅名 → ゾーン index（CORRIDORS の各 base 内）。
const CORRIDOR_ZONE = {} // base -> { 駅名: zoneIndex }
for (const [base, zones] of Object.entries(CORRIDORS)) {
  const m = (CORRIDOR_ZONE[base] = {})
  zones.forEach((z, i) => z.stations.forEach((s) => (m[s] = i)))
}

/**
 * 物理路線の一部区間を別サービス名で描き替える（CORRIDORS ほど複雑でない補完用）。
 * 両端の最寄り駅がともに stations に含まれるセグメントだけ対象。本数はサービス名で CSV からひく。
 * 総武線の緩行（御茶ノ水〜錦糸町）＝中央・総武線各駅停車、快速地下線（東京〜錦糸町）＝総武線快速。
 * （錦糸町〜千葉の複々線は従来どおり SERVICE_LINES で描く。）
 */
const STATION_RELABEL = {
  総武線: [
    // 快速地下線（東京〜錦糸町）。新日本橋・馬喰町は快速線のみの駅なのでアンカー
    // （片端がこれらに最寄りなら快速）。地下線は両国の真下を通り端点が両国にスナップ
    // することがあるため、両端一致だけだと取りこぼす。
    { line: '総武線快速', color: '#0072BC', anchors: new Set(['新日本橋', '馬喰町']), stations: new Set(['東京', '新日本橋', '馬喰町', '錦糸町']) },
    { line: '中央・総武線各駅停車', color: '#FFD400', stations: new Set(['御茶ノ水', '秋葉原', '浅草橋', '両国', '錦糸町']) },
  ],
}

/**
 * 私鉄の路線表示名の補正（事業者名|N02路線名 → 表示名）。
 * 「本線」「1号線」など事業者をまたいで重複・曖昧な名前を分かりやすくする。
 */
const LINE_NAME_OVERRIDES = {
  '京成電鉄|本線': '京成本線',
  '京成電鉄|千葉線': '京成千葉線',
  '京成電鉄|千原線': '京成千原線',
  '京成電鉄|東成田線': '京成東成田線',
  '京成電鉄|成田空港線': '成田スカイアクセス線',
  '千葉都市モノレール|1号線': '千葉モノレール1号線',
  '千葉都市モノレール|2号線': '千葉モノレール2号線',
  '東京地下鉄|5号線東西線': '東京メトロ東西線',
  '東京都|10号線新宿線': '都営新宿線',
  '東武鉄道|野田線': '東武野田線',
  '東武鉄道|伊勢崎線': '東武伊勢崎線',
  '東武鉄道|東上本線': '東武東上線',
  '東武鉄道|日光線': '東武日光線',
  '東武鉄道|亀戸線': '東武亀戸線',
  '東武鉄道|越生線': '東武越生線',
  // 東武（北関東の支線）
  '東武鉄道|佐野線': '東武佐野線',
  '東武鉄道|小泉線': '東武小泉線',
  '東武鉄道|桐生線': '東武桐生線',
  '東武鉄道|鬼怒川線': '東武鬼怒川線',
  '京成電鉄|押上線': '京成押上線',
  '京成電鉄|金町線': '京成金町線',
  '首都圏新都市鉄道|常磐新線': 'つくばエクスプレス',
  // 赤羽線（池袋〜赤羽）は埼京線の一部。通称へ統一（赤羽〜大宮別線は東北線 CORRIDORS で）。
  '東日本旅客鉄道|赤羽線': '埼京線',
  // 在圏内で別事業者が同名になる路線の分離（統合されると別路線が 1 本になる）。
  '京浜急行電鉄|本線': '京急本線',
  '京浜急行電鉄|大師線': '京急大師線',
  '東武鉄道|大師線': '東武大師線',
  // 東京メトロ（N02 は「N号線◯◯線」表記。東西線は上で定義済み）。
  '東京地下鉄|3号線銀座線': '東京メトロ銀座線',
  '東京地下鉄|4号線丸ノ内線': '東京メトロ丸ノ内線',
  '東京地下鉄|4号線丸ノ内線分岐線': '東京メトロ丸ノ内線',
  '東京地下鉄|2号線日比谷線': '東京メトロ日比谷線',
  '東京地下鉄|9号線千代田線': '東京メトロ千代田線',
  '東京地下鉄|8号線有楽町線': '東京メトロ有楽町線',
  '東京地下鉄|11号線半蔵門線': '東京メトロ半蔵門線',
  '東京地下鉄|7号線南北線': '東京メトロ南北線',
  '東京地下鉄|13号線副都心線': '東京メトロ副都心線',
  // 都営地下鉄・都電（新宿線は上で定義済み）。
  '東京都|1号線浅草線': '都営浅草線',
  '東京都|6号線三田線': '都営三田線',
  '東京都|12号線大江戸線': '都営大江戸線',
  '東京都|荒川線': '都電荒川線',
  // 横浜市営地下鉄（1号線＋3号線＝ブルーライン、4号線＝グリーンライン）。
  '横浜市|1号線': '横浜市営ブルーライン',
  '横浜市|3号線': '横浜市営ブルーライン',
  '横浜市|4号線': '横浜市営グリーンライン',
  // 京急（本線・大師線は上で定義済み）。
  '京浜急行電鉄|空港線': '京急空港線',
  '京浜急行電鉄|久里浜線': '京急久里浜線',
  '京浜急行電鉄|逗子線': '京急逗子線',
  // 西武（他社にも同名・あいまい名がある路線を明示）。
  '西武鉄道|池袋線': '西武池袋線',
  '西武鉄道|新宿線': '西武新宿線',
  '西武鉄道|多摩川線': '西武多摩川線',
  '西武鉄道|山口線': '西武山口線',
  // 小田急（湘南モノレール「江の島線」と紛らわしいため）。
  '小田急電鉄|江ノ島線': '小田急江ノ島線',
  // 新交通・モノレール・登山鉄道などの分かりにくい N02 名。
  'ゆりかもめ|東京臨海新交通臨海線': 'ゆりかもめ',
  '東京臨海高速鉄道|臨海副都心線': 'りんかい線',
  '湘南モノレール|江の島線': '湘南モノレール',
  '江ノ島電鉄|江ノ島電鉄線': '江ノ島電鉄',
  '埼玉新都市交通|伊奈線': 'ニューシャトル',
  '箱根登山鉄道|鉄道線': '箱根登山鉄道',
  '箱根登山鉄道|鋼索線': '箱根登山ケーブルカー',
  '御岳登山鉄道|ケーブルカー': '御岳登山ケーブルカー',
  '高尾登山電鉄|高尾鋼索線': '高尾登山ケーブルカー',
  '大山観光電鉄|大山鋼索線': '大山ケーブルカー',
  '伊豆箱根鉄道|大雄山線': '伊豆箱根大雄山線',

  // ===== 東北への拡大で追加 =====
  // 仙台市地下鉄（N02 は「南北線」「東西線」表記。横浜市営と同様に事業者名を明示）。
  '仙台市|南北線': '仙台市営南北線',
  '仙台市|東西線': '仙台市営東西線',
  // 三陸鉄道: N02 は移管時期の異なる区間ごとに「北リアス線」「南リアス線」「リアス線」の
  // 旧称が混在するが、2019年に全区間が「リアス線」へ統一されているため表示名を統一する。
  '三陸鉄道|北リアス線': '三陸鉄道リアス線',
  '三陸鉄道|南リアス線': '三陸鉄道リアス線',
  '三陸鉄道|リアス線': '三陸鉄道リアス線',

  // ===== 北海道への拡大で追加 =====
  // 札幌市営地下鉄（N02 は「南北線」「東西線」「東豊線」表記）。
  '札幌市|南北線': '札幌市営南北線',
  '札幌市|東西線': '札幌市営東西線',
  '札幌市|東豊線': '札幌市営東豊線',
  // 札幌市電: N02 は開業時期の異なる区間ごとに「1条線」「山鼻線」「山鼻西線」「都心線」に
  // 分かれているが、現在は一体の環状路線として運行されているため表示名を統一する。
  '一般社団法人札幌市交通事業振興公社|1条線': '札幌市電',
  '一般社団法人札幌市交通事業振興公社|山鼻線': '札幌市電',
  '一般社団法人札幌市交通事業振興公社|山鼻西線': '札幌市電',
  '一般社団法人札幌市交通事業振興公社|都心線': '札幌市電',
  // 函館市電: N02 は「大森線」「宝来・谷地頭線」「本線」「湯の川線」に分かれているが、
  // 一体の路線網として運行されているため表示名を統一する。
  '函館市|大森線': '函館市電',
  '函館市|宝来・谷地頭線': '函館市電',
  '函館市|本線': '函館市電',
  '函館市|湯の川線': '函館市電',

  // ===== 中部への拡大で追加 =====
  // 中央線・東海道線は関東側が CORRIDORS（系統別の通称）で描かれるため、同名の
  // JR東海・JR西日本区間をそのまま同名にすると誤って関東の通称ゾーンに吸われてしまう。
  // 事業者ごとに表示名を分けて CORRIDORS の対象から外す（本数は別個に扱う）。
  '東海旅客鉄道|中央線': '中央線(JR東海)',
  '東海旅客鉄道|東海道線': '東海道線(JR東海)',
  '西日本旅客鉄道|東海道線': '東海道線(JR西日本)',
  // 名鉄・南海に同名「空港線」があるため事業者名を明示。
  '名古屋鉄道|空港線': '名鉄空港線',
  // 名古屋市営地下鉄（N02 は路線番号付き表記。4号線名城線は2号線名城線と同じ環状路線）。
  '名古屋市|1号線東山線': '名古屋市営東山線',
  '名古屋市|2号線名城線': '名古屋市営名城線',
  '名古屋市|2号線名港線': '名古屋市営名港線',
  '名古屋市|3号線鶴舞線': '名古屋市営鶴舞線',
  '名古屋市|4号線名城線': '名古屋市営名城線',
  '名古屋市|6号線桜通線': '名古屋市営桜通線',
  '名古屋市|上飯田線': '名古屋市営上飯田線',
  // あおなみ線（通称）。
  '名古屋臨海高速鉄道|西名古屋港線': 'あおなみ線',
  // リニモ（通称）。
  '愛知高速交通|東部丘陵線': 'リニモ',
  // 「本線」「鉄道線」は事業者をまたいで曖昧なため明示（黒部峡谷鉄道|本線 と富山地方鉄道|本線 の衝突回避）。
  '富山地方鉄道|本線': '富山地方鉄道本線',
  '黒部峡谷鉄道|本線': '黒部峡谷鉄道線',
  '富山地方鉄道|支線': '富山地方鉄道支線',
  '立山黒部貫光|鋼索線': '立山黒部貫光鋼索線',
  '遠州鉄道|鉄道線': '遠州鉄道線',

  // ===== 近畿への拡大で追加 =====
  // 大阪メトロ（4号線中央線は関東の中央線 CORRIDORS との衝突回避のため必須）。
  '大阪市高速電気軌道|1号線(御堂筋線)': '大阪メトロ御堂筋線',
  '大阪市高速電気軌道|2号線(谷町線)': '大阪メトロ谷町線',
  '大阪市高速電気軌道|3号線(四つ橋線)': '大阪メトロ四つ橋線',
  '大阪市高速電気軌道|4号線(中央線)': '大阪メトロ中央線',
  '大阪市高速電気軌道|5号線(千日前線)': '大阪メトロ千日前線',
  '大阪市高速電気軌道|6号線(堺筋線)': '大阪メトロ堺筋線',
  '大阪市高速電気軌道|7号線(長堀鶴見緑地線)': '大阪メトロ長堀鶴見緑地線',
  '大阪市高速電気軌道|8号線(今里筋線)': '大阪メトロ今里筋線',
  '大阪モノレール|国際文化公園都市モノレール線(彩都線)': '大阪モノレール彩都線',
  // 京都市営地下鉄（他都市の東西線と衝突するため事業者名を明示）。
  '京都市|東西線': '京都市営東西線',
  '京都市|烏丸線': '京都市営烏丸線',
  // 神戸市営地下鉄: 北神線・西神線・西神延伸線・山手線は一体運行の「西神・山手線」に統合。
  '神戸市|北神線': '神戸市営西神・山手線',
  '神戸市|山手線': '神戸市営西神・山手線',
  '神戸市|西神線': '神戸市営西神・山手線',
  '神戸市|西神延伸線': '神戸市営西神・山手線',
  '神戸市|海岸線': '神戸市営海岸線',
  // 近鉄・阪急・南海・北大阪急行に同名路線があるため事業者名を明示。
  '近畿日本鉄道|京都線': '近鉄京都線',
  '近畿日本鉄道|奈良線': '近鉄奈良線',
  '近畿日本鉄道|長野線': '近鉄長野線',
  '近畿日本鉄道|山田線': '近鉄山田線',
  '阪急電鉄|京都線': '阪急京都線',
  '南海電気鉄道|空港線': '南海空港線',
  '北大阪急行電鉄|南北線': '北大阪急行南北線',
  // ケーブルカー（鋼索線）は事業者ごとに別施設のため明示。
  '京福電気鉄道|鋼索線': '京福鋼索線',
  '京阪電気鉄道|鋼索線': '京阪鋼索線',
  '南海電気鉄道|鋼索線': '南海鋼索線',
  // 「本線」は事業者をまたいで曖昧なため明示。
  '山陽電気鉄道|本線': '山陽電鉄本線',
  '阪神電気鉄道|本線': '阪神本線',
  '近江鉄道|本線': '近江鉄道本線',

  // ===== 中国5県への拡大で追加 =====
  '広島電鉄|本線': '広島電鉄本線',
  '広島高速交通|広島新交通1号線': 'アストラムライン',

  // ===== 四国4県への拡大で追加 =====
  // 伊予鉄道の城北線は東海交通事業の城北線（愛知）と衝突するため明示。
  '伊予鉄道|城北線': '伊予鉄道城北線',
  // とさでん交通は開業時期の異なる区間ごとの表示名だが一体運行のため統一。
  'とさでん交通|伊野線': 'とさでん交通',
  'とさでん交通|後免線': 'とさでん交通',
  'とさでん交通|桟橋線': 'とさでん交通',
  'とさでん交通|駅前線': 'とさでん交通',

  // ===== 九州7県・沖縄への拡大で追加 =====
  '北九州高速鉄道|小倉線': '北九州モノレール',
  '沖縄都市モノレール|沖縄都市モノレール線': 'ゆいレール',
  // 熊本市電・鹿児島市電: 開業時期の異なる区間ごとの表示名だが一体運行のため統一。
  '熊本市|上熊本線': '熊本市電',
  '熊本市|健軍線': '熊本市電',
  '熊本市|幹線': '熊本市電',
  '熊本市|水前寺線': '熊本市電',
  '熊本市|田崎線': '熊本市電',
  '鹿児島市|唐湊線': '鹿児島市電',
  '鹿児島市|第一期線': '鹿児島市電',
  '鹿児島市|第二期線': '鹿児島市電',
  '鹿児島市|谷山線': '鹿児島市電',
  // 甘木鉄道と西鉄に同名「甘木線」があるため事業者名を明示。
  '西日本鉄道|甘木線': '西鉄甘木線',
  // 長崎電気軌道の「本線」は他事業者と衝突するため明示。
  '長崎電気軌道|本線': '長崎電気軌道本線',
  // 福岡市営地下鉄（N02 は路線番号付き表記）。
  '福岡市|1号線(空港線)': '福岡市営空港線',
  '福岡市|2号線(箱崎線)': '福岡市営箱崎線',
  '福岡市|3号線(七隈線)': '福岡市営七隈線',
}

/** 路線カラー（白地図上で識別しやすい配色）。表示名でひく。 */
const LINE_COLORS = {
  // JR 東日本
  総武線: '#F2C500',
  京葉線: '#C9252F',
  外房線: '#E8382C',
  内房線: '#1E78C8',
  成田線: '#2EA84F',
  東金線: '#F08300',
  久留里線: '#D6418D',
  武蔵野線: '#8E44AD',
  常磐線: '#00A0A0',
  鹿島線: '#7F5A3C',
  // 私鉄・三セク
  京成本線: '#005BAC',
  京成千葉線: '#3DA9E0',
  京成千原線: '#67A82E',
  京成東成田線: '#9E7BB5',
  成田スカイアクセス線: '#EE7800',
  北総線: '#13469B',
  新京成線: '#E85298',
  東京メトロ東西線: '#009BBF',
  都営新宿線: '#6CBB5A',
  東武野田線: '#54B948',
  東葉高速線: '#B5651D',
  千葉モノレール1号線: '#00838F',
  千葉モノレール2号線: '#4DB6AC',
  つくばエクスプレス: '#1B2F8A',
  小湊鐵道線: '#D7003A',
  いすみ線: '#F2B500',
  ユーカリが丘線: '#8C8C8C',
  ディズニーリゾートライン: '#FF77AA',
  銚子電気鉄道線: '#7A4FBF',
  芝山鉄道線: '#9A9A33',
  流山線: '#C2185B',

  // ===== 南関東への拡大で追加（各社公式のラインカラーをベースに） =====
  // JR 東日本（駅ナンバリングのラインカラー）
  山手線: '#9ACD32',
  根岸線: '#00BAE8', // 京浜東北・根岸線（横浜〜大船。京浜東北は CORRIDORS で別途）
  高崎線: '#F68B1E',
  青梅線: '#F15A22',
  五日市線: '#F15A22',
  横須賀線: '#0072BC', // 大船〜久里浜（東京方の品鶴線は東海道線 CORRIDORS で別途）
  横浜線: '#80C342',
  南武線: '#FBD05D',
  鶴見線: '#FFD400',
  八高線: '#A8A39D',
  川越線: '#00AC9A', // 川越線・埼京線
  埼京線: '#00AC9A', // 池袋〜赤羽（赤羽線をリネーム。赤羽〜大宮別線は東北線 CORRIDORS）
  湘南新宿ライン: '#F68B1E',
  相模線: '#009793',
  // 東北線・東海道線・中央線は系統が重なるため CORRIDORS（後述）で通称別に描く
  京浜東北線: '#00BAE8',
  宇都宮線: '#F68B1E',
  東海道線: '#F68B1E',
  中央線快速: '#F15A22',
  '中央・総武線各駅停車': '#FFD400',
  '相鉄・JR直通線': '#6A1B7A',
  東北新幹線: '#2CA13A',
  上越新幹線: '#2CA13A',
  // JR 東海
  東海道新幹線: '#2E5BA0',
  御殿場線: '#F77F00',
  // 東京メトロ（公式コーポレートカラー）
  東京メトロ銀座線: '#FF9500',
  東京メトロ丸ノ内線: '#F62E36',
  東京メトロ日比谷線: '#B5B5AC',
  東京メトロ千代田線: '#00BB85',
  東京メトロ有楽町線: '#C1A470',
  東京メトロ半蔵門線: '#8F76D6',
  東京メトロ南北線: '#00ADA9',
  東京メトロ副都心線: '#9C5E31',
  // 都営地下鉄・都電
  都営浅草線: '#E85298',
  都営三田線: '#0079C2',
  都営大江戸線: '#B6007A',
  都電荒川線: '#E60012',
  // 横浜市営地下鉄
  横浜市営ブルーライン: '#0070C0',
  横浜市営グリーンライン: '#00A650',
  金沢シーサイドライン: '#0091D2',
  // 京王
  京王線: '#DD0077',
  高尾線: '#DD0077',
  相模原線: '#DD0077',
  競馬場線: '#DD0077',
  動物園線: '#DD0077',
  井の頭線: '#0079C3',
  // 小田急
  小田原線: '#0067C0',
  小田急江ノ島線: '#0067C0',
  多摩線: '#0067C0',
  // 東急
  東横線: '#DA0442',
  目黒線: '#009CD2',
  田園都市線: '#20A23B',
  大井町線: '#F18D00',
  池上線: '#EE86A8',
  東急多摩川線: '#B4007F',
  世田谷線: '#FABE00',
  こどもの国線: '#6BBF4B',
  東急新横浜線: '#00A0B0',
  // 京急（空港線のみエアポート青、他は京急レッド）
  京急本線: '#E60012',
  京急久里浜線: '#E60012',
  京急逗子線: '#E60012',
  京急大師線: '#E60012',
  京急空港線: '#0099D9',
  // 西武（西武ブルー）
  西武池袋線: '#00A0E9',
  西武新宿線: '#00A0E9',
  西武秩父線: '#00A0E9',
  拝島線: '#00A0E9',
  多摩湖線: '#00A0E9',
  西武多摩川線: '#00A0E9',
  国分寺線: '#00A0E9',
  西武山口線: '#00A0E9',
  狭山線: '#00A0E9',
  西武園線: '#00A0E9',
  西武有楽町線: '#00A0E9',
  豊島線: '#00A0E9',
  // 東武（東武ブルー。野田線は上で定義済み）
  東武伊勢崎線: '#006FBC',
  東武東上線: '#006FBC',
  東武日光線: '#006FBC',
  東武亀戸線: '#006FBC',
  東武大師線: '#006FBC',
  東武越生線: '#006FBC',
  // 相鉄（YOKOHAMA NAVYBLUE）
  相鉄本線: '#002B7F',
  相鉄いずみ野線: '#002B7F',
  相鉄新横浜線: '#002B7F',
  // 京成（押上線・金町線は京成ブルー）
  京成押上線: '#005BAC',
  京成金町線: '#005BAC',
  // 新交通・モノレール・その他
  ゆりかもめ: '#0068B7',
  りんかい線: '#00A0C6',
  東京モノレール羽田線: '#C8102E',
  多摩都市モノレール線: '#6FB92C',
  埼玉高速鉄道線: '#0086CB',
  ニューシャトル: '#6CBE45',
  '日暮里・舎人ライナー': '#C9258B',
  みなとみらい21線: '#0B318F',
  江ノ島電鉄: '#009844',
  湘南モノレール: '#2B57A6',
  箱根登山鉄道: '#E60012',
  箱根登山ケーブルカー: '#F39800',
  御岳登山ケーブルカー: '#009844',
  高尾登山ケーブルカー: '#E60012',
  大山ケーブルカー: '#006FBC',
  伊豆箱根大雄山線: '#1267B5',
  秩父本線: '#00913A',

  // ===== 北関東（茨城・栃木・群馬）への拡大で追加 =====
  // JR 東日本（駅ナンバリング／路線記号のラインカラーをベースに）
  両毛線: '#FCC800',
  水戸線: '#EA5504',
  水郡線: '#00A7DB',
  烏山線: '#00A9A5',
  日光線: '#B07C52', // レトロ調ブラウン
  上越線: '#EA5504',
  信越線: '#00A650',
  吾妻線: '#EF858C',
  北陸新幹線: '#00609C',
  // 東武（東武ブルー）
  東武佐野線: '#006FBC',
  東武小泉線: '#006FBC',
  東武桐生線: '#006FBC',
  東武鬼怒川線: '#006FBC',
  // 私鉄・三セク（各社コーポレート／ラインカラーをベースに）
  わたらせ渓谷線: '#6BA644', // 渓谷グリーン
  上信線: '#F39800', // 上信電鉄
  上毛線: '#0068B7', // 上毛電気鉄道
  会津鬼怒川線: '#E5006D', // 野岩鉄道
  大洗鹿島線: '#00A0E9', // 鹿島臨海鉄道
  宇都宮芳賀ライトレール線: '#FABE00', // ライトライン・イエロー
  常総線: '#0079C2', // 関東鉄道
  竜ヶ崎線: '#0079C2', // 関東鉄道
  湊線: '#EE7800', // ひたちなか海浜鉄道
  真岡線: '#D7003A', // 真岡鐵道（SL レッド）
  筑波山鋼索鉄道線: '#7FA82B', // 筑波観光鉄道ケーブルカー

  // ===== 東北6県への拡大で追加 =====
  // JR 東日本（公式の駅ナンバリング等は無い区間が多いため、路線の地域性・愛称を踏まえた配色）
  奥羽線: '#C9A227',
  五能線: '#1E90C8',
  仙山線: '#8FBC3F',
  仙石線: '#00A6E9',
  八戸線: '#3F51B5',
  北上線: '#8B5A2B',
  只見線: '#4C9F70',
  大湊線: '#4169E1',
  大船渡線: '#20B2AA',
  山田線: '#CD5B45',
  左沢線: '#8CC63F',
  気仙沼線: '#00B2A9',
  津軽線: '#5B4E8C',
  田沢湖線: '#C0392B',
  男鹿線: '#1B75BB',
  石巻線: '#26A69A',
  磐越東線: '#5FA777',
  磐越西線: '#7B4F2C',
  米坂線: '#D4A017',
  羽越線: '#008081',
  花輪線: '#8B6F4E',
  釜石線: '#4682B4',
  陸羽東線: '#E08E45',
  陸羽西線: '#4A90D9',
  // JR 東日本（東北本線。系統が重なるため CORRIDORS で通称別に描く。色はそちらで指定）
  // 私鉄・三セク
  いわて銀河鉄道線: '#0056A7',
  青い森鉄道線: '#0091DA',
  三陸鉄道リアス線: '#E2001A',
  仙台空港線: '#0095D9',
  会津線: '#0068B6',
  フラワー長井線: '#E4007F',
  弘南線: '#E60012',
  大鰐線: '#C1272D',
  津軽鉄道線: '#B22222',
  鳥海山ろく線: '#F39800',
  飯坂線: '#F4A900',
  秋田内陸線: '#2E8B57',
  阿武隈急行線: '#8FC31F',
  仙台市営南北線: '#00A73E',
  仙台市営東西線: '#7B2D8E',

  // ===== 北海道への拡大で追加 =====
  // JR 北海道
  函館線: '#004098',
  北海道新幹線: '#2CA13A', // 東北新幹線と直通のため同系色
  千歳線: '#00A0DE',
  宗谷線: '#3399CC',
  室蘭線: '#1F8A70',
  富良野線: '#8E5FA8', // ラベンダー畑の沿線イメージ
  日高線: '#E4A11B',
  札沼線: '#3CB371', // 愛称「学園都市線」のグリーン
  根室線: '#A0522D',
  海峡線: '#2F4F6F',
  留萌線: '#D2A106',
  石勝線: '#2E7D32',
  石北線: '#41729F',
  釧網線: '#4FA89B',
  // 私鉄・三セク
  道南いさりび鉄道線: '#0070AD',
  札幌市営南北線: '#00A040',
  札幌市営東西線: '#F39800',
  札幌市営東豊線: '#0074BF',
  札幌市電: '#E2001A',
  函館市電: '#1B7340',

  // ===== 中部9県への拡大で追加 =====
  // JR 東日本（新潟・長野・山梨方面。上越線・信越線は北関東拡大時に定義済み）
  中央線: '#F15A22', // 高尾以西の延伸区間（中央線快速の CORRIDORS ゾーンで別途 trainSection 分割）
  伊東線: '#00A0C6',
  大糸線: '#4CAF93',
  小海線: '#5B8DB8',
  弥彦線: '#D9A441',
  白新線: '#3E8E7E',
  篠ノ井線: '#6A8CAF',
  越後線: '#2F9C95',
  飯山線: '#7EA6C9',
  // JR 東海
  '中央線(JR東海)': '#D2691E',
  '東海道線(JR東海)': '#F68B1E',
  太多線: '#B8860B',
  武豊線: '#5C9EAD',
  身延線: '#8E6C88', // 関西線は近畿拡大時に定義済み
  飯田線: '#A0785C',
  高山線: '#3F7CAC',
  // JR 西日本
  '東海道線(JR西日本)': '#F68B1E',
  七尾線: '#0075C1',
  // 私鉄・三セク（北陸）
  IRいしかわ鉄道線: '#00A0DC',
  あいの風とやま鉄道線: '#003DA5',
  妙高はねうまライン: '#3CB878',
  日本海ひすいライン: '#00A99D',
  三国芦原線: '#F7941D',
  勝山永平寺線: '#E8890C',
  ほくほく線: '#F39800',
  浅野川線: '#EE7800',
  石川線: '#F4A100',
  富山地方鉄道本線: '#D0021B',
  富山地方鉄道支線: '#D0021B',
  上滝線: '#D0021B',
  不二越線: '#D0021B',
  呉羽線: '#D0021B',
  安野屋線: '#D0021B',
  富山港線: '#D0021B',
  富山都心線: '#D0021B',
  富山駅南北接続線: '#D0021B',
  立山線: '#D0021B',
  黒部峡谷鉄道線: '#F5A623',
  立山黒部貫光鋼索線: '#4A90D9',
  無軌条電車線: '#6BAED6',
  福武線: '#F26522',
  // 私鉄・三セク（長野・上田・松本）
  しなの鉄道線: '#8FAA36',
  北しなの線: '#6E9B3B',
  上高地線: '#6DBE45',
  別所線: '#8B3A3A',
  長野線: '#D0021B',
  // 私鉄・三セク（東海）
  伊豆急行線: '#E85298',
  駿豆線: '#1267B5', // 伊豆箱根大雄山線は南関東拡大時に定義済み
  大井川本線: '#B33B3B',
  井川線: '#8B5E3C',
  天竜浜名湖線: '#4C9F5D',
  遠州鉄道線: '#E4002B',
  静岡清水線: '#F39800',
  岳南鉄道線: '#F5A623',
  '大月線': '#0068B7',
  '河口湖線': '#0068B7',
  樽見線: '#4C8C4A',
  養老線: '#F4A900',
  明知線: '#8C6239',
  城北線: '#7089A0',
  // 私鉄・三セク（名古屋圏）
  名古屋本線: '#EE3124',
  三河線: '#EE3124',
  各務原線: '#EE3124',
  小牧線: '#EE3124',
  尾西線: '#EE3124',
  常滑線: '#EE3124',
  広見線: '#EE3124',
  河和線: '#EE3124',
  津島線: '#EE3124',
  瀬戸線: '#EE3124',
  犬山線: '#EE3124',
  知多新線: '#EE3124',
  竹鼻線: '#EE3124',
  築港線: '#EE3124',
  羽島線: '#EE3124',
  蒲郡線: '#EE3124',
  西尾線: '#EE3124',
  豊川線: '#EE3124',
  豊田線: '#EE3124',
  名鉄空港線: '#EE3124',
  名古屋線: '#00305F', // 近鉄ネイビー
  愛知環状鉄道線: '#00A0DC',
  あおなみ線: '#00A6D6',
  リニモ: '#00ADA9',
  名古屋市営東山線: '#FFD400',
  名古屋市営名城線: '#7D3F98',
  名古屋市営名港線: '#9B6FB5',
  名古屋市営鶴舞線: '#0072BC',
  名古屋市営桜通線: '#EE3124',
  名古屋市営上飯田線: '#7C90A0',
  ガイドウェイバス志段味線: '#64B5CD',
  新湊港線: '#00A6A6',
  高岡軌道線: '#00A6A6',
  越美南線: '#8FBC3F',
  十国鋼索線: '#4A90D9',
  城端線: '#D9A441',
  小浜線: '#6A8CAF',
  氷見線: '#5C9EAD',
  越美北線: '#8FBC3F',
  東田本線: '#F5A623', // 豊橋鉄道市内線
  渥美線: '#F5A623',
  本四備讃線: '#0072BC',

  // ===== 近畿7府県への拡大で追加 =====
  // JR 西日本
  大阪環状線: '#F68B1E',
  'JR東西線': '#0086D1',
  おおさか東線: '#EE7800',
  加古川線: '#8FBC3F',
  北陸線: '#00609C',
  姫新線: '#B08968',
  山陰線: '#00A0DE',
  山陽新幹線: '#0068B7',
  山陽線: '#0072BC',
  播但線: '#A0785C',
  桜井線: '#C9A227',
  桜島線: '#00A0C6',
  湖西線: '#4A90D9',
  片町線: '#8FAA36',
  福知山線: '#5B8DB8',
  舞鶴線: '#6A8CAF',
  草津線: '#7EA6C9',
  赤穂線: '#D9A441',
  関西空港線: '#0072BC',
  阪和線: '#0091D2',
  和歌山線: '#8C6239',
  奈良線: '#7B4B94',
  紀勢線: '#B33B3B',
  関西線: '#8B5E3C',
  // JR 東海
  参宮線: '#8E6C88',
  名松線: '#A0785C',
  // 近鉄（近鉄マルーン。けいはんな線のみ実車両の青系統）
  大阪線: '#A30021',
  南大阪線: '#A30021',
  吉野線: '#A30021',
  天理線: '#A30021',
  志摩線: '#A30021',
  橿原線: '#A30021',
  湯の山線: '#A30021',
  生駒線: '#A30021',
  生駒鋼索線: '#A30021',
  田原本線: '#A30021',
  西信貴鋼索線: '#A30021',
  道明寺線: '#A30021',
  鈴鹿線: '#A30021',
  難波線: '#A30021',
  鳥羽線: '#A30021',
  御所線: '#A30021',
  信貴線: '#A30021',
  近鉄京都線: '#A30021',
  近鉄奈良線: '#A30021',
  近鉄長野線: '#A30021',
  近鉄山田線: '#A30021',
  けいはんな線: '#0072BC',
  // 京阪（京阪グリーン）
  京阪本線: '#00913A',
  交野線: '#00913A',
  京津線: '#3FA050',
  宇治線: '#00913A',
  石山坂本線: '#3FA050',
  中之島線: '#00913A',
  鴨東線: '#00913A',
  京阪鋼索線: '#4A90D9',
  // 阪急（阪急マルーン）
  阪急京都線: '#7A3B2E',
  今津線: '#7A3B2E',
  伊丹線: '#7A3B2E',
  千里線: '#7A3B2E',
  宝塚線: '#7A3B2E',
  嵐山線: '#7A3B2E',
  甲陽線: '#7A3B2E',
  神戸線: '#7A3B2E',
  箕面線: '#7A3B2E',
  // 阪神（阪神ネイビー）
  阪神本線: '#0F4C9C',
  武庫川線: '#0F4C9C',
  阪神なんば線: '#0F4C9C',
  // 南海（南海グリーン）
  南海本線: '#00A65E',
  高野線: '#00A65E',
  加太線: '#00A65E',
  和歌山港線: '#00A65E',
  多奈川線: '#00A65E',
  高師浜線: '#00A65E',
  南海空港線: '#00A65E',
  南海鋼索線: '#4A90D9',
  // 神戸高速線（阪急・阪神・神戸電鉄の共同使用区間）
  神戸高速線: '#7A3B2E',
  // 山陽電気鉄道
  山陽電鉄本線: '#004098',
  網干線: '#004098',
  // 神戸電鉄
  三田線: '#00A73E',
  公園都市線: '#00A73E',
  有馬線: '#00A73E',
  粟生線: '#00A73E',
  // 大阪メトロ
  大阪メトロ御堂筋線: '#E5171F',
  大阪メトロ谷町線: '#864099',
  大阪メトロ四つ橋線: '#0089D2',
  大阪メトロ中央線: '#00A650',
  大阪メトロ千日前線: '#EC6EA3',
  大阪メトロ堺筋線: '#623812',
  大阪メトロ長堀鶴見緑地線: '#82C31C',
  大阪メトロ今里筋線: '#E5A100',
  南港ポートタウン線: '#F39800',
  大阪モノレール線: '#F5A623',
  大阪モノレール彩都線: '#F5A623',
  // 京都市営地下鉄
  京都市営東西線: '#7B2D8E',
  京都市営烏丸線: '#00A73E',
  // 神戸市営地下鉄
  '神戸市営西神・山手線': '#00A040',
  神戸市営海岸線: '#0072BC',
  ポートアイランド線: '#0072BC',
  六甲アイランド線: '#F39800',
  // 阪堺電気軌道
  阪堺線: '#F39800',
  上町線: '#F39800',
  // 京福電気鉄道（嵐電）
  北野線: '#DA5297',
  嵐山本線: '#DA5297',
  京福鋼索線: '#4A90D9',
  // 叡山電鉄
  叡山本線: '#E4002B',
  鞍馬線: '#E4002B',
  // 三岐鉄道
  三岐線: '#0072BC',
  北勢線: '#F39800',
  近鉄連絡線: '#A30021',
  // 近江鉄道
  近江鉄道本線: '#0072BC',
  八日市線: '#0072BC',
  多賀線: '#0072BC',
  // 能勢電鉄
  妙見線: '#4C9F5D',
  日生線: '#4C9F5D',
  // その他
  智頭線: '#8C6239',
  伊賀線: '#0072BC',
  伊勢線: '#8FBC3F',
  信楽線: '#F39800',
  北条線: '#8C6239',
  水間線: '#00913A',
  紀州鉄道線: '#D7003A',
  貴志川線: '#F39800', // 「たま駅長」の和歌山電鐵
  泉北高速鉄道線: '#0091D2',
  北大阪急行南北線: '#E5171F',
  嵯峨野観光線: '#8C6239',
  内部線: '#F39800',
  八王子線: '#F39800',
  宮津線: '#00609C', // 京都丹後鉄道
  宮福線: '#00609C',
  摩耶ケーブル線: '#8C6239',
  六甲ケーブル線: '#8C6239',
  比叡山鉄道線: '#4A90D9',
  鞍馬山鋼索鉄道: '#4A90D9',
  天橋立鋼索鉄道: '#4A90D9',

  // ===== 中国5県への拡大で追加 =====
  広島短距離交通瀬野線: '#F5A623', // スカイレール
  北松江線: '#4A90D9',
  大社線: '#0072BC',
  井原線: '#8FBC3F',
  東山本線: '#F39800', // 岡山電気軌道
  清輝橋線: '#F39800',
  宇品線: '#00913A', // 広島電鉄
  宮島線: '#00913A',
  広島電鉄本線: '#00913A',
  横川線: '#00913A',
  江波線: '#00913A',
  白島線: '#00913A',
  皆実線: '#00913A',
  アストラムライン: '#0072BC',
  水島本線: '#8C6239',
  若桜線: '#D7003A',
  伯備線: '#3F7CAC',
  可部線: '#8FAA36',
  吉備線: '#C9A227', // 桃太郎線
  呉線: '#00A0C6',
  因美線: '#7EA6C9',
  境線: '#4FA89B', // 鬼太郎列車
  宇部線: '#5C9EAD',
  宇野線: '#8FBC3F',
  小野田線: '#6A8CAF',
  山口線: '#B08968',
  岩徳線: '#A0785C',
  木次線: '#8B6F4E',
  津山線: '#D9A441',
  福塩線: '#7EA6C9',
  美祢線: '#8C6239',
  芸備線: '#8B5E3C',
  錦川清流線: '#4C9F5D',

  // ===== 四国4県への拡大で追加 =====
  とさでん交通: '#00913A',
  伊予鉄道城北線: '#F39800',
  城南線: '#F39800',
  大手町線: '#F39800',
  本町線: '#F39800',
  横河原線: '#F39800',
  花園線: '#F39800',
  連絡線: '#F39800',
  郡中線: '#F39800',
  高浜線: '#F39800',
  八栗ケーブル: '#4A90D9',
  予土線: '#4FA89B',
  予讃線: '#0072BC',
  内子線: '#8FBC3F',
  土讃線: '#D7003A',
  徳島線: '#8FAA36',
  牟岐線: '#00A0C6',
  高徳線: '#5C9EAD',
  鳴門線: '#6A8CAF',
  中村線: '#8C6239',
  宿毛線: '#8C6239',
  阿佐線: '#8C6239',
  阿佐東線: '#F39800',
  志度線: '#F5A623', // ことでん
  琴平線: '#F5A623',
  長尾線: '#F5A623',

  // ===== 九州7県・沖縄への拡大で追加 =====
  湯前線: '#4C9F5D',
  別府ラクテンチケーブル線: '#F5A623',
  // JR 九州
  三角線: '#00A0C6',
  久大線: '#8FAA36',
  九州新幹線: '#E60012', // つばめレッド
  西九州新幹線: '#E60012',
  佐世保線: '#5C9EAD',
  吉都線: '#8B6F4E',
  唐津線: '#6A8CAF',
  大村線: '#00A99D',
  宮崎空港線: '#0072BC',
  後藤寺線: '#8C6239',
  指宿枕崎線: '#F39800', // 指宿カラー
  日南線: '#00A65E',
  日田彦山線: '#7EA6C9',
  日豊線: '#0072BC',
  筑肥線: '#4FA89B',
  筑豊線: '#8B5E3C',
  篠栗線: '#D9A441',
  肥薩線: '#A0785C',
  豊肥線: '#B08968',
  長崎線: '#0091D2',
  香椎線: '#3E8E7E',
  鹿児島線: '#0072BC',
  博多南線: '#0072BC',
  // 私鉄・三セク
  北九州モノレール: '#F5A623',
  高森線: '#8FBC3F',
  島原鉄道線: '#D7003A',
  伊田線: '#8C6239', // 平成筑豊鉄道
  田川線: '#8C6239',
  糸田線: '#8C6239',
  門司港レトロ観光線: '#F39800',
  西九州線: '#0075C1', // 松浦鉄道
  ゆいレール: '#E85298',
  熊本市電: '#00913A',
  菊池線: '#4C9F5D', // 熊本電気鉄道
  藤崎線: '#4C9F5D',
  甘木線: '#8FBC3F',
  西鉄甘木線: '#0F4C9C',
  帆柱ケーブル線: '#4A90D9',
  福岡市営空港線: '#F62E36',
  福岡市営箱崎線: '#0072BC',
  福岡市営七隈線: '#00A040',
  筑豊電気鉄道線: '#00913A',
  肥薩おれんじ鉄道線: '#F39800',
  天神大牟田線: '#0F4C9C', // 西鉄
  太宰府線: '#0F4C9C',
  貝塚線: '#0F4C9C',
  長崎電気軌道本線: '#00913A',
  大浦支線: '#00913A',
  桜町支線: '#00913A',
  蛍茶屋支線: '#00913A',
  赤迫支線: '#00913A',
  鹿児島市電: '#00913A',
}
const DEFAULT_COLOR = '#666666'

/** 乗換駅に加えて主要駅（ターミナル・拠点）としてラベルを出す駅。 */
const MAJOR_STATIONS = new Set([
  // JR
  '千葉', '船橋', '西船橋', '津田沼', '稲毛', '市川', '本八幡',
  '松戸', '柏', '我孫子', '成田', '成田空港', '空港第２ビル',
  '佐倉', '銚子', '八日市場', '旭', '成東', '東金', '大網',
  '茂原', '上総一ノ宮', '勝浦', '安房鴨川', '館山', '君津',
  '木更津', '五井', '蘇我', '大原', '久留里', '上総亀山',
  '香取', '佐原', '松岸', '新松戸', '海浜幕張', '舞浜',
  // 私鉄の拠点
  '京成船橋', '京成津田沼', '京成成田', '京成千葉', '千葉中央',
  '新鎌ヶ谷', '勝田台', '八千代台', '北習志野', '流山おおたかの森',
  'ユーカリが丘', '五香',
  // 北関東の拠点（単独駅で乗換判定に載らない主要駅）
  '前橋', '土浦', '日光', '足利', '大洗', '桐生', '高崎', '宇都宮', '水戸',
  '東武宇都宮', '西桐生', '中央前橋', '常陸太田', '常陸大宮', '阿字ケ浦',
  // 東北の拠点（単独駅で乗換判定に載らない主要駅・県庁所在地・新幹線停車駅など）
  '福島', '郡山', '会津若松', 'いわき', '白河',
  '仙台', '石巻', '気仙沼', '大船渡', '一ノ関', '古川',
  '山形', '米沢', '新庄', '酒田', '鶴岡',
  '秋田', '大館', '横手', '角館',
  '盛岡', '花巻', '北上', '宮古', '久慈', '釜石',
  '青森', '弘前', '八戸', '五所川原', '大湊',
  // 北海道の拠点（単独駅で乗換判定に載らない主要駅・道庁所在地・新幹線/主要都市駅など）
  '札幌', '函館', '小樽', '旭川', '帯広', '釧路', '北見', '網走',
  '稚内', '苫小牧', '室蘭', '新千歳空港', '新函館北斗', '富良野', '名寄', '夕張',
  // 中部の拠点（単独駅で乗換判定に載らない主要駅・県庁所在地・新幹線停車駅など）
  '新潟', '長岡', '上越妙高', '富山', '高岡', '金沢', '小松', '福井', '敦賀',
  '甲府', '長野', '松本', '上田', '飯田', '岐阜', '高山', '静岡', '浜松', '沼津',
  '名古屋', '豊橋', '岡崎', '一宮', '半田',
  // 近畿の拠点（単独駅で乗換判定に載らない主要駅・府県庁所在地・新幹線停車駅など）
  '津', '四日市', '伊勢', '鳥羽', '大津', '彦根', '草津',
  '京都', '大阪', '新大阪', '梅田', 'なんば', '天王寺', '神戸', '三宮', '姫路',
  '奈良', '橿原神宮前', '和歌山', '新宮',
  // 中国地方の拠点（単独駅で乗換判定に載らない主要駅・県庁所在地・新幹線停車駅など）
  '鳥取', '米子', '境港', '松江', '出雲市', '益田',
  '岡山', '倉敷', '広島', '呉', '福山', '尾道', '三次',
  '山口', '下関', '徳山', '岩国', '宇部',
  // 四国の拠点（単独駅で乗換判定に載らない主要駅・県庁所在地・新幹線代替の拠点など）
  '徳島', '高松', '松山', '高知', '今治', '新居浜', '丸亀', '宇和島',
  // 九州・沖縄の拠点（単独駅で乗換判定に載らない主要駅・県庁所在地・新幹線停車駅など）
  '博多', '小倉', '北九州', '久留米', '佐賀', '長崎', '佐世保', '大村',
  '熊本', '八代', '大分', '別府', '宮崎', '延岡', '鹿児島中央', '西鹿児島',
  '那覇', '沖縄',
])

function load(p) {
  return JSON.parse(fs.readFileSync(p, 'utf8'))
}

// --- 対象都県ポリゴンによる内外判定（複数都県の和集合） ---
function buildPrefTest(japan, ids) {
  const polys = []
  for (const id of ids) {
    const pref = japan.features.find((f) => f.properties.id === id)
    if (!pref) throw new Error(`都県フィーチャが見つかりません: id=${id}`)
    const g = pref.geometry
    if (g.type === 'Polygon') polys.push(g.coordinates)
    else if (g.type === 'MultiPolygon') g.coordinates.forEach((p) => polys.push(p))
  }

  let minX = Infinity, minY = Infinity, maxX = -Infinity, maxY = -Infinity
  polys.forEach((p) =>
    p[0].forEach(([x, y]) => {
      if (x < minX) minX = x
      if (y < minY) minY = y
      if (x > maxX) maxX = x
      if (y > maxY) maxY = y
    }),
  )
  const inRing = (x, y, ring) => {
    let inside = false
    for (let i = 0, j = ring.length - 1; i < ring.length; j = i++) {
      const xi = ring[i][0], yi = ring[i][1]
      const xj = ring[j][0], yj = ring[j][1]
      const hit = yi > y !== yj > y && x < ((xj - xi) * (y - yi)) / (yj - yi) + xi
      if (hit) inside = !inside
    }
    return inside
  }
  return (x, y) => {
    if (x < minX || x > maxX || y < minY || y > maxY) return false
    for (const poly of polys) {
      if (inRing(x, y, poly[0])) {
        let hole = false
        for (let k = 1; k < poly.length; k++) {
          if (inRing(x, y, poly[k])) { hole = true; break }
        }
        if (!hole) return true
      }
    }
    return false
  }
}

/** LineString の中点（代表点）。 */
function midPoint(coords) {
  return coords[Math.floor(coords.length / 2)]
}

/** 座標列の重心（駅の代表点に使う）。 */
function centroid(coords) {
  let sx = 0, sy = 0
  for (const [x, y] of coords) { sx += x; sy += y }
  return [sx / coords.length, sy / coords.length]
}

/** N02 の事業者名・路線名から表示用の路線名を決める。 */
function displayLineName(operator, rawName) {
  return LINE_NAME_OVERRIDES[`${operator}|${rawName}`] || rawName
}

/**
 * 並走（line-offset）の左右が描画方向で決まるため、セグメントの向きを揃える。
 * 主軸が東西なら西→東、南北なら南→北に統一（東北線のような南北路線にも対応）。
 */
function orientConsistent(coords) {
  const a = coords[0], b = coords[coords.length - 1]
  const dx = Math.abs(b[0] - a[0]), dy = Math.abs(b[1] - a[1])
  if (dx >= dy) return a[0] <= b[0] ? coords : coords.slice().reverse()
  return a[1] <= b[1] ? coords : coords.slice().reverse()
}

function main() {
  const japan = load(JAPAN_GEOJSON)
  const inTarget = buildPrefTest(japan, TARGET_PREF_IDS)
  const rs = load(path.join(N02_DIR, 'N02-23_RailroadSection.geojson'))
  const st = load(path.join(N02_DIR, 'N02-23_Station.geojson'))

  // 駅名 → 区間ラベル（区間定義のある JR 路線のみ）。
  const stationToSection = {} // 路線名 -> { 駅名: label }
  for (const [line, secs] of Object.entries(LINE_SECTIONS)) {
    const m = (stationToSection[line] = {})
    // 先書き優先: 複数区間に現れる境界駅は最初の区間（より手前）に属させる。
    // 境界セグメント検出（後述）と組み合わせ、セグメントを正しい区間に割り当てる。
    for (const s of secs) for (const name of s.stations) if (!(name in m)) m[name] = s.label
  }
  // 区間割り当て用に、区間定義のある路線（事業者問わず）の駅座標を集める。
  // CORRIDORS の base 路線は、ゾーンに登録した駅のみ集めて zone も持たせる。
  const lineStations = {} // 表示路線名 -> [{ name, x, y }]
  const corridorStations = {} // base -> [{ name, x, y, zone }]
  for (const f of st.features) {
    if (EXCLUDED_OPERATORS.has(f.properties.N02_004)) continue
    const ln = displayLineName(f.properties.N02_004, f.properties.N02_003)
    const [cx, cy] = centroid(f.geometry.coordinates)
    if (!inTarget(cx, cy)) continue
    if (LINE_SECTIONS[ln] || SERVICE_LINES[ln]) {
      ;(lineStations[ln] = lineStations[ln] || []).push({ name: f.properties.N02_005, x: cx, y: cy })
    }
    if (CORRIDORS[ln]) {
      const zone = CORRIDOR_ZONE[ln][f.properties.N02_005]
      if (zone !== undefined)
        ;(corridorStations[ln] = corridorStations[ln] || []).push({ name: f.properties.N02_005, x: cx, y: cy, zone })
    }
  }
  // 点に最も近い駅名を返す。
  const nearestStation = (line, x, y) => {
    let best = null, bestD = Infinity
    for (const s of lineStations[line] || []) {
      const d = (s.x - x) ** 2 + (s.y - y) ** 2
      if (d < bestD) { bestD = d; best = s.name }
    }
    return best
  }
  // CORRIDORS: 点の最寄りゾーン駅から、その通称ゾーン定義を返す（駅・点用）。
  const corridorOf = (base, x, y) => {
    let best = null, bestD = Infinity
    for (const s of corridorStations[base] || []) {
      const d = (s.x - x) ** 2 + (s.y - y) ** 2
      if (d < bestD) { bestD = d; best = s.zone }
    }
    return CORRIDORS[base][best ?? 0]
  }
  // CORRIDORS（セグメント用）: 中点でゾーンを決めるが、端点が分岐線（品鶴・埼京・相鉄）
  // の駅に接する区間は分岐ゾーンを優先する。分岐線は主線（鶴見・川崎・赤羽等）の近くを
  // 通るため、中点だけだと主線ゾーン（京浜東北＋東海道 等）へ誤って吸われてしまう。
  const corridorOfSegment = (base, coords) => {
    const a = coords[0], b = coords[coords.length - 1]
    for (const [x, y] of [a, b]) {
      const z = corridorOf(base, x, y)
      if (z.branch) return z
    }
    const [mx, my] = midPoint(coords)
    return corridorOf(base, mx, my)
  }
  const sectionOf = (line, x, y) =>
    (stationToSection[line][nearestStation(line, x, y)] || '') || LINE_SECTIONS[line][0].label
  // セグメントが並走サービス区間（複々線）に入っているか。
  // 両端の最寄り駅がともに区間内のときだけ並走にする（境界の外側へはみ出さない）。
  const inServiceZone = (line, coords) => {
    const set = SERVICE_STATION_SET[line]
    if (!set) return false
    const a = coords[0]
    const b = coords[coords.length - 1]
    const na = nearestStation(line, a[0], a[1])
    const nb = nearestStation(line, b[0], b[1])
    return !!na && !!nb && set.has(na) && set.has(nb)
  }

  // --- 路線: 対象都県内のセグメントを出力先（区間ラベル / 並走サービス / 路線名）へ
  //     振り分けて MultiLineString へまとめる。並走サービスは 1 セグメントを複数出力 ---
  // 採否は「いずれかの頂点が圏内（約1kmのバッファ込み）」。中点だけだと東京湾の
  // 埋立地・人工島を渡る区間（ゆりかもめのお台場ループ、東京モノレールの整備場〜
  // 昭和島など）の頂点が、海上で粗い県境ポリゴンの外に落ちて区間が欠落するため、
  // 県境から少し外側まで含める。
  // 約0.8km。湾岸の埋立地区間を拾いつつ、隣県（茨城の常総線など）を巻き込まない値。
  const BORDER_BUFFER = 0.008
  const nearTarget = (x, y) =>
    inTarget(x, y) ||
    inTarget(x + BORDER_BUFFER, y) ||
    inTarget(x - BORDER_BUFFER, y) ||
    inTarget(x, y + BORDER_BUFFER) ||
    inTarget(x, y - BORDER_BUFFER)
  const lines = {} // キー -> { coords, category, line, color, section?, offset? }
  for (const f of rs.features) {
    const op = f.properties.N02_004
    if (EXCLUDED_OPERATORS.has(op)) continue
    if (!f.geometry.coordinates.some(([x, y]) => nearTarget(x, y))) continue
    const [mx, my] = midPoint(f.geometry.coordinates)
    const name = displayLineName(op, f.properties.N02_003)
    const category = JR_OPERATORS.has(op) ? 'jr' : 'private'

    // out: { line(表示名), trainKey(本数キー), color, offset, section? }
    const coords = f.geometry.coordinates
    let outs, relabel
    if (CORRIDORS[name]) {
      // 系統が重なる JR 幹線: セグメントの通称ゾーンの各サービスを引く（共用区間は offset で並走）。
      const zone = corridorOfSegment(name, coords)
      outs = zone.services.map((sv) => {
        const sec = sv.trainSection ? `${sv.name}(${sv.trainSection})` : undefined
        return { line: sv.name, trainKey: sec || sv.name, section: sec, color: sv.color, offset: sv.offset }
      })
    } else if (
      STATION_RELABEL[name] &&
      (relabel = (() => {
        const a = nearestStation(name, coords[0][0], coords[0][1])
        const b = nearestStation(name, coords[coords.length - 1][0], coords[coords.length - 1][1])
        if (!a || !b) return null
        return STATION_RELABEL[name].find(
          (r) =>
            (r.anchors && (r.anchors.has(a) || r.anchors.has(b))) ||
            (r.stations.has(a) && r.stations.has(b)),
        )
      })())
    ) {
      outs = [{ line: relabel.line, trainKey: relabel.line, color: relabel.color }]
    } else if (SERVICE_LINES[name] && inServiceZone(name, f.geometry.coordinates)) {
      outs = SERVICE_LINES[name].map((sv) => ({ line: sv.name, trainKey: sv.name, color: sv.color, offset: sv.offset }))
    } else if (LINE_SECTIONS[name]) {
      // 中点判定では区間境界をまたぐセグメントの割り当てが不安定になる。
      // 両端の最寄り駅が異なる区間に属するとき（境界セグメント）は、
      // CSV の記載順で後ろ（より遠方）の区間に割り当て、境界を明確にする。
      const stmap = stationToSection[name]
      const na = nearestStation(name, coords[0][0], coords[0][1])
      const nb = nearestStation(name, coords[coords.length - 1][0], coords[coords.length - 1][1])
      const sa = na ? stmap[na] : undefined
      const sb = nb ? stmap[nb] : undefined
      let section
      if (sa && sb && sa !== sb) {
        const idxA = LINE_SECTIONS[name].findIndex((s) => s.label === sa)
        const idxB = LINE_SECTIONS[name].findIndex((s) => s.label === sb)
        section = idxA < idxB ? sb : sa
      } else {
        section = sectionOf(name, mx, my)
      }
      outs = [{ line: name, trainKey: section, color: LINE_COLORS[name] || DEFAULT_COLOR, section }]
    } else {
      outs = [{ line: name, trainKey: name, color: LINE_COLORS[name] || DEFAULT_COLOR }]
    }
    for (const o of outs) {
      // 同一サービスでも offset が違えば別フィーチャ（共用区間=offset / 単独区間=中央寄せ）。
      // 凡例は line 名で重複排除されるので分割しても 1 項目に集約される。
      const key = `${o.section || o.line}#${o.offset || 0}`
      const e = (lines[key] =
        lines[key] || { coords: [], category, line: o.line, color: o.color, section: o.section, offset: o.offset, trainKey: o.trainKey })
      // 並走サービスは line-offset の左右が描画方向で決まるため、
      // 全セグメントの向きを揃えて各停/快速等の側が反転しないようにする。
      const coords = o.offset ? orientConsistent(f.geometry.coordinates) : f.geometry.coordinates
      e.coords.push(coords)
    }
  }
  const railwayFeatures = Object.keys(lines).map((key) => {
    const e = lines[key]
    // 区間分割された路線の SERVICE_LINES/CORRIDORS 参照では trainKey がサービス名（区間なし）に
    // なるため、セクションキーの最初の一致をフォールバックとして使う。
    // CORRIDORS ゾーン別キー（例: 東海道線(東京〜横浜)）の場合はサービス名ベース（東海道線）も試みる。
    const resolveTrains = (key) => {
      if (TRAINS[key] !== undefined) return TRAINS[key]
      const prefixMatch = Object.values(
        Object.fromEntries(Object.entries(TRAINS).filter(([k]) => k.startsWith(key + '(')))
      )[0]
      if (prefixMatch !== undefined) return prefixMatch
      const m = /^(.+)\([^)]+\)$/.exec(key)
      if (m && TRAINS[m[1]] !== undefined) return TRAINS[m[1]]
      return DEFAULT_TRAINS
    }
    const props = { line: e.line, category: e.category, color: e.color, trains: resolveTrains(e.trainKey) }
    if (e.section) props.section = e.section
    if (e.offset) props.offset = e.offset
    return { type: 'Feature', properties: props, geometry: { type: 'MultiLineString', coordinates: e.coords } }
  })
  // 並びを JR → 私鉄、路線名、区間 にする。
  railwayFeatures.sort((a, b) => {
    if (a.properties.category !== b.properties.category)
      return a.properties.category === 'jr' ? -1 : 1
    if (a.properties.line !== b.properties.line)
      return a.properties.line.localeCompare(b.properties.line, 'ja')
    return (a.properties.section || '').localeCompare(b.properties.section || '', 'ja')
  })

  // --- 駅: 駅名で集約。ただし同名でも離れた別駅は別点に分ける ---
  // N02 は同一物理駅を事業者・路線ごとに別フィーチャで持つので駅名で寄せる。
  // ただし広域化すると同名・別location の駅（例: 栄町＝千葉モノレール/都電荒川線、
  // 永田＝外房線/秩父鉄道）が混ざるため、駅名の中で近接クラスタ単位に分けて
  // それぞれ 1 点にする（霞ヶ関のメトロ3線は同一駅、東武東上の霞ヶ関は別点、を満たす）。
  // 駅の所属路線名も、地図と同じ通称へ寄せる（東北線→京浜東北線/宇都宮線、
  // 東海道線の品鶴→横須賀線、総武線の御茶ノ水〜錦糸町→中央・総武各停 等）。
  const reclassStationLines = (base, x, y, stName) => {
    if (CORRIDORS[base]) return [...new Set(corridorOf(base, x, y).services.map((s) => s.name))]
    const rel = STATION_RELABEL[base]
    if (rel) {
      const ls = [...new Set(rel.filter((r) => r.stations.has(stName)).map((r) => r.line))]
      if (ls.length) return ls
    }
    return [base]
  }
  const stationRecords = {} // 駅名 -> [{ x, y, line, jr }]
  for (const f of st.features) {
    if (EXCLUDED_OPERATORS.has(f.properties.N02_004)) continue
    const [cx, cy] = centroid(f.geometry.coordinates)
    if (!inTarget(cx, cy)) continue
    const op = f.properties.N02_004
    const jr = JR_OPERATORS.has(op)
    const base = displayLineName(op, f.properties.N02_003)
    const recs = (stationRecords[f.properties.N02_005] = stationRecords[f.properties.N02_005] || [])
    for (const ln of reclassStationLines(base, cx, cy, f.properties.N02_005)) {
      recs.push({ x: cx, y: cy, line: ln, jr })
    }
  }
  // 同名の駅レコードを近接クラスタへ分割（単連結・貪欲法）。
  const clusterRecords = (recs) => {
    const clusters = []
    for (const r of recs) {
      let target = null
      for (const cl of clusters) {
        if (cl.some((p) => Math.hypot(p.x - r.x, p.y - r.y) <= STATION_CLUSTER_DIST)) {
          target = cl
          break
        }
      }
      if (target) target.push(r)
      else clusters.push([r])
    }
    return clusters
  }
  const stationFeatures = []
  for (const [name, recs] of Object.entries(stationRecords)) {
    for (const cl of clusterRecords(recs)) {
      const [x, y] = centroid(cl.map((p) => [p.x, p.y]))
      const lineList = [...new Set(cl.map((p) => p.line))]
      const major = lineList.length > 1 || MAJOR_STATIONS.has(name)
      const stop = stopTrainsFor(name, lineList)
      stationFeatures.push({
        type: 'Feature',
        properties: {
          name,
          lines: lineList,
          isJr: cl.some((p) => p.jr),
          isPrivate: cl.some((p) => !p.jr),
          major,
          // stopTrains=合計（駅サイズ用）、stopTypes=種別内訳の JSON 文字列（ポップアップ用）。
          ...(stop ? { stopTrains: stop.total, stopTypes: JSON.stringify(stop.types) } : {}),
        },
        geometry: { type: 'Point', coordinates: [x, y] },
      })
    }
  }
  stationFeatures.sort((a, b) => a.properties.name.localeCompare(b.properties.name, 'ja'))

  fs.mkdirSync(OUT_DIR, { recursive: true })
  const railways = { type: 'FeatureCollection', features: railwayFeatures }
  const stations = { type: 'FeatureCollection', features: stationFeatures }
  fs.writeFileSync(path.join(OUT_DIR, 'railways.geojson'), JSON.stringify(railways))
  fs.writeFileSync(path.join(OUT_DIR, 'stations.geojson'), JSON.stringify(stations))

  const jrLines = railwayFeatures.filter((f) => f.properties.category === 'jr')
  const pvLines = railwayFeatures.filter((f) => f.properties.category === 'private')
  const majorCount = stationFeatures.filter((f) => f.properties.major).length
  console.log(`路線フィーチャ: JR ${jrLines.length} / 私鉄 ${pvLines.length}`)
  railwayFeatures.forEach((f) => {
    const p = f.properties
    const label = p.section ? `${p.line} ／ ${p.section}` : p.line
    console.log(`  [${p.category}] ${String(p.trains).padStart(4)}本  ${label}`)
  })
  console.log(`駅: ${stationFeatures.length}（主要 ${majorCount}）`)
}

main()
